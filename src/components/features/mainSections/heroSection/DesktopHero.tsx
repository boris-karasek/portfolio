import { HeroContent } from "@/components/features//mainSections/heroSection/HeroContent";

export const DesktopHero = () => {
  return (
    <section
      id="hero"
      className="px-32 py-24 bg-transparent"
    >
      <HeroContent />
    </section>
  );
};
